####### Xyra Voice — Kamailio (Step 3: Call Routing + WebRTC) #########
#
# Routes calls between registered SIP users (browser softphones).
# Uses rtpengine to bridge WebRTC DTLS-SRTP to plain RTP.
#
########################################################################

import_file "/etc/kamailio/secrets/kamailio-db.cfg"

#!define WS_PORT 8088
#!define RTPENGINE_SOCK "udp:127.0.0.1:22222"

####### Global Parameters ##########

debug=2
log_stderror=yes
log_facility=LOG_LOCAL0
fork=no
children=2  # Keep small to stay within Supavisor pool limits

# Required for xhttp / WebSocket
tcp_accept_no_cl=yes
tcp_connection_lifetime=3604

# Listen on all interfaces
listen=udp:0.0.0.0:5060
listen=tcp:0.0.0.0:5060
listen=tcp:0.0.0.0:8088   # Plain WebSocket — Nginx terminates TLS in front

####### Modules Section ##########

mpath="/usr/lib/kamailio/modules/"

# Core
loadmodule "tm.so"
loadmodule "sl.so"
loadmodule "rr.so"
loadmodule "pv.so"
loadmodule "maxfwd.so"
loadmodule "textops.so"
loadmodule "siputils.so"
loadmodule "xlog.so"
loadmodule "sanity.so"
loadmodule "ctl.so"
loadmodule "kex.so"
loadmodule "corex.so"

# Database
loadmodule "db_postgres.so"

# Auth
loadmodule "auth.so"
loadmodule "auth_db.so"

# Registration / location
loadmodule "usrloc.so"
loadmodule "registrar.so"

# Dialog management & relay
loadmodule "dialog.so"

# WebSocket / HTTP
loadmodule "xhttp.so"
loadmodule "websocket.so"

# NAT / RTP
loadmodule "nathelper.so"
loadmodule "rtpengine.so"

####### Module Parameters ##########

# Routing
modparam("rr", "enable_full_lr", 1)
modparam("rr", "append_fromtag", 0)

# usrloc — keep registrations in memory only
modparam("usrloc", "db_mode", 0)
modparam("usrloc", "nat_bflag", 7)

# registrar
modparam("registrar", "method_filtering", 1)
modparam("registrar", "max_expires", 3600)
modparam("registrar", "min_expires", 60)
modparam("registrar", "default_expires", 3600)
modparam("registrar", "received_avp", "$avp(received_uri)")

# auth_db
modparam("auth_db", "db_url", DBURL)
modparam("auth_db", "calculate_ha1", 1)
modparam("auth_db", "use_domain", 0)
modparam("auth_db", "user_column", "sip_username")
modparam("auth_db", "password_column", "sip_password")
modparam("auth_db", "load_credentials", "")

# rtpengine
modparam("rtpengine", "rtpengine_sock", RTPENGINE_SOCK)
modparam("rtpengine", "extra_id_pv", "$avp(extra_id)")

# nathelper
modparam("nathelper", "natping_interval", 30)
modparam("nathelper", "ping_nated_only", 1)
modparam("nathelper", "sipping_bflag", 7)
modparam("nathelper", "sipping_from", "sip:pinger@sip.xyrachat.com")
modparam("nathelper", "received_avp", "$avp(received_uri)")

####### Routing Logic ##########

request_route {
    # ─── Sanity ───
    if (!mf_process_maxfwd_header("10")) {
        sl_send_reply("483", "Too Many Hops");
        exit;
    }
    if (!sanity_check("17895", "7")) {
        xlog("L_WARN", "Malformed SIP from $si:$sp\n");
        exit;
    }

    # ─── Health checks ───
    if (is_method("OPTIONS") && uri == myself) {
        sl_send_reply("200", "Xyra Voice — Kamailio Online");
        exit;
    }

    # ─── In-dialog requests (BYE, ACK, re-INVITE, etc.) ───
    if (has_totag()) {
        if (loose_route()) {
            route(RELAY);
            exit;
        }
        sl_send_reply("404", "Not here");
        exit;
    }

    # CANCEL — handled by tm
    if (is_method("CANCEL")) {
        if (t_check_trans()) {
            route(RELAY);
        }
        exit;
    }

    # ─── REGISTER ───
    if (is_method("REGISTER")) {
        if (!auth_check("$fd", "sip_users", "1")) {
            auth_challenge("$fd", "0");
            exit;
        }
        consume_credentials();

        # Mark contacts behind NAT (most browsers are)
        if (nat_uac_test("19")) {
            fix_nated_register();
            setbflag(7);
        }

        if (!save("location")) {
            sl_reply_error();
        }
        exit;
    }

    # ─── INVITE ───
    if (is_method("INVITE")) {
        # Authenticate caller
        if (!auth_check("$fd", "sip_users", "1")) {
            auth_challenge("$fd", "0");
            exit;
        }
        consume_credentials();

        # Lookup destination in usrloc (find where the callee is registered)
        if (!lookup("location")) {
            sl_send_reply("404", "User not registered");
            exit;
        }

        xlog("L_INFO", "INVITE: $fU -> $rU (registered at $du)\n");

        # Mark whether this leg needs WebRTC bridging
        route(NATMANAGE);
        route(RTPENGINE_OFFER);
        route(RELAY);
        exit;
    }

    # ─── MESSAGE / SUBSCRIBE / NOTIFY ───
    if (is_method("MESSAGE|SUBSCRIBE|NOTIFY")) {
        if (!auth_check("$fd", "sip_users", "1")) {
            auth_challenge("$fd", "0");
            exit;
        }
        consume_credentials();

        if (!lookup("location")) {
            sl_send_reply("404", "User not registered");
            exit;
        }

        route(RELAY);
        exit;
    }

    sl_send_reply("405", "Method Not Allowed");
    exit;
}

# ─── Relay route ───
route[RELAY] {
    if (!t_relay()) {
        sl_reply_error();
    }
    exit;
}

# ─── NAT detection / fix-up ───
route[NATMANAGE] {
    if (nat_uac_test("19")) {
        fix_nated_contact();
        setbflag(6);  # mark as NAT
    }
    # Browsers are always WebSocket
    if (proto == WS || proto == WSS) {
        setbflag(8);  # mark as WebRTC
    }
}

# ─── rtpengine: rewrite SDP for media bridging ───
route[RTPENGINE_OFFER] {
    # If caller is WebRTC (browser), de-encrypt + transcode RTCP-mux
    if (isbflagset(8)) {
        rtpengine_offer("RTP/AVP replace-origin replace-session-connection ICE=remove");
    } else {
        rtpengine_offer("replace-origin replace-session-connection");
    }
}

# Replies to outgoing INVITEs — fix NAT and bridge media
onreply_route {
    if (status =~ "(180)|(183)|(2[0-9][0-9])") {
        if (nat_uac_test("19")) {
            fix_nated_contact();
        }
        # If destination is WebRTC, encrypt media back to it
        if (isbflagset(8)) {
            rtpengine_answer("RTP/SAVPF rtcp-mux ICE=force");
        } else {
            rtpengine_answer();
        }
    }
    xlog("L_INFO", "Reply: $rs $rr from $si\n");
}

# ─── BYE/CANCEL: tell rtpengine to release media ───
event_route[tm:branch-failure] {
    rtpengine_delete();
}

# ─── WebSocket handshake ─────────────────────────────────
event_route[xhttp:request] {
    if ($hdr(Upgrade) =~ "websocket" && $hdr(Connection) =~ "Upgrade") {
        ws_handle_handshake();
        exit;
    }
    xhttp_reply("404", "Not Found", "text/plain", "Xyra Voice");
}
