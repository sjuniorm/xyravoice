; ============================================================
; Step 1: Asterisk dialplan placeholder
; Step 2 will add real call flow logic
; ============================================================

[general]
static=yes
writeprotect=no

[default]
exten => _X.,1,NoOp(Xyra Voice — call received: ${EXTEN})
 same => n,Answer()
 same => n,Playback(hello-world)
 same => n,Hangup()
